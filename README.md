# zenoGrid — single-table data grid

A stripped-down Table on top of the zenoGrid API. See **NOTES.md**
for what's built, what's not, and where to pick up next.

## Run
use node 20
use nvm to switch to 20 version if not installed
npm install
npm run dev

Open http://localhost:3000
